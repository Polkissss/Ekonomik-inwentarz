services:
  - type: web
    name: ekonomik-inwentarz
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    build:
      pythonVersion: 3.12.10